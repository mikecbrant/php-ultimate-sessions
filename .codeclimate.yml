engines:
  duplication:
    enabled: true
    config:
      languages:
      - php
  fixme:
    enabled: true
  phpmd:
    enabled: true
ratings:
  paths:
    - "**UltimateSessionConfig.php"
exclude_paths:
  - build/
  - tests/
  - vendor/
